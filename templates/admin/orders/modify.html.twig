{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ title }}{% endblock %}

{% block body %}
 
    <section id="adminOrderModify">
    
            {{include('partials/_adminNavbarMenu.html.twig')}}

        <div class="container-fluid wrapper">
            
            {{include('partials/_adminSidebarMenu.html.twig')}}
            
            <div class="formWrap">
            <h4>Modification de la commande <i># {{order.reference}}</i> </h4>
            <h5 class="mt-3"><i class="fa-solid fa-truck-fast"></i> Mise à jour du statut de la livraison</h5>
                <div class="form">
                    {{form_start(form)}}
                        {{form_row(form.delivery_status)}}
                        {{form_row(form.trackerID)}}  
                        <h5 class="mt-3"><i class="fa-solid fa-map-location-dot"></i> Modification de l'adresse de livraison</h5>
                        {{form_row(form.address, {'label':false})}}
                        {{form_row(form.addressComplement, {'label':false})}}
                        {{form_row(form.zip, {'label':false})}}
                        {{form_row(form.city, {'label':false})}}
                        <div>
                            <a href="{{path('admin_orders')}}" class="btn btn-dark"><i class="fa-solid fa-chevron-left"></i> Retour</a>
                            <button type="submit" id="modifyButton" class="btn btn-warning m-3">Modifier</button>
                        </div>

                    {{form_end(form)}}
                        
                           
                </div>

            </div>
               
                   
            </div>
                
        </div>
 
    </section>
    <script>

     
{% endblock %}





