
    <video id="player" playsinline controls data-poster="{{asset('uploads/article/video/thumb/' ~ video.thumbnail )}}" data-plyr-config='{ "title": "{{video.title}}"}'>
        <source src="{{asset('uploads/article/video/' ~ video.source )}}" type="video/mp4" />
    </video>
    {# Vérification si il y a un titre de vidéo (pour éviter des décalages css) #}
    {# {% if video.title %}
    <div class=" p-0 ">
        <div class="row">
            <div class="col-md-8 my-1">
                
                <h5 class="pt-3 ms-3">{{video.title}}</h5>
                
                {# NB : Parti pris de mettre les views de manière globale sur l'article plutot que sur la vidéo #}
                
                {# <div class="text-muted">
                <small>{{video.views}}{% if video.views <= 1 %} vue {% else %} vues {% endif %} • {{video.createdAt|date('d/m/Y')}}</small>
                </div> 
            </div>
        </div>
    </div>
    {% endif %} #}

