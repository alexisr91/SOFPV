
<div class="card">
    <video id="player" playsinline controls data-poster="{{asset('uploads/article/video/thumb/' ~ video.thumbnail )}}">
        <source src="{{asset('uploads/article/video/' ~ video.source )}}" type="video/mp4" />
    </video>
    <div class="card-body p-0 ">
        <div class="row">
            <div class="col-md-8 my-1">
                <h5 class="card-title ps-3 pt-3">{{video.title}}</h5>
                <div class="card-text text-muted ps-3">
                <small>{{video.views}}{% if video.views <= 1 %} vue {% else %} vues {% endif %} • {{video.createdAt|date('d/m/Y')}}</small>
                </div>
            </div>
        </div>
    </div>
</div>

